{
  "name": "fx-sync-cron-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker that pings the fx-sync cron route at a punctual local time",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --test-scheduled",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250109.0",
    "typescript": "^5.7.3",
    "wrangler": "^3.101.0"
  }
}
